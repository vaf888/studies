#include <iostream>
#include <cassert>
#include <cstring> // memset

using namespace std;


//What is the output of the program below?




int main()
{
	//The nested conditional statement shown below has been written by an 
	//inexperienced C/C++ programmer. The behavior of the statement is not 
	//correctly represented by the formatting.
	if (n < 10)
		if (n > 0)
	cout << "The number is positive." << endl;
	else
		cout << "The number is ______________." << endl;

	// What is the output of the statement 
    // if the variable n has the value 7 ? If n has the value 15 ?

	//	If n has the value -3 ?

	//	b. Correct the syntax of the statement so that the logic of the 
    //  corrected statement corresponds to the
	//  formatting of the original statement. 
    // Also, replace the blank with an appropriate word or phrase.
	
	//c. Correct the formatting of the (original) statement so that the new format reflects the logical behavior
	//   of the original statement. Also, replace the blank with an appropriate word or phrase.
	
}